# ironelf R3 修复回调结果说明提示

把下面这段直接发给 `ironelf` 开发线程即可。

---

请基于 `joint-issue` 文档完成 R3 Worker 路径修复后，按下面模板回调结果。不要只说已修复，需要给出可核验事实。

## 你需要阅读的文档

1. `/Users/sourcefire/X-lab/ironelf/docs/Issue-Checks/2026-03/joint-issue/01-联合定位修复报告-S9-RuntimeBridge-Ironelf-2026-03-29.md`
2. `/Users/sourcefire/X-lab/ironelf/docs/Issue-Checks/2026-03/joint-issue/02-联合回测用例说明-S9-RuntimeBridge-Ironelf-2026-03-29.md`

## 修复目标

- 不回退 R3 分流：`web_search` 与 `web_fetch` 继续走 `Worker`
- 修掉当前 `Worker` 路径 blocker，并以最新联合回测口径为准
- 保证失败时 receipt 或 event 原因可诊断

## 回调结果必须包含

1. 分支名与 commit hash
2. 变更文件清单
3. 根因说明
4. 修复点说明
5. web_fetch 直连 runtime 回测结果
6. web_search 直连 runtime 回测结果
7. browser 路径确认结果
8. `/v1/models` 检查结果
9. `chimera-core` 联合回测结果
10. 剩余风险

## 回调格式

### 1. 本次提交

- branch:
- commit:
- changed files:

### 2. 根因与修复

- 根因：
- 修复点：
- 是否改动了 `runtime_bridge.rs` 分流规则：是 或 否
- 若有改动，说明为什么不会把 `web_search` 与 `web_fetch` 回退到 `browser`

### 3. 直连回测结果

#### A. web_fetch

- execution_id:
- submit: PASS / FAIL
- job_mode 是否为 `worker`: PASS / FAIL
- done: PASS / FAIL
- terminal_state:
- execution_state:
- summary:

#### B. web_search

- execution_id:
- submit: PASS / FAIL
- job_mode 是否为 `worker`: PASS / FAIL
- done: PASS / FAIL
- terminal_state:
- execution_state:
- summary:
- 是否返回显式来源列表: PASS / FAIL
- sources:

#### C. browser

- execution_id:
- job_mode 是否保持浏览器路径: PASS / FAIL
- summary:

### 4. 联合回测结果

请用 `chimera-core` 侧样例做联合回测，并回填：

- search 样例：`请执行发布检查，并检索 Rust 官方网站收集证据`
- fetch 样例：`请执行发布检查，并 fetch https://www.rust-lang.org/ 并总结页面证据`
- 强约束 search 样例：必须返回来源列表；如果没有来源则视为未通过

回填格式：

- search hint:
- search lane:
- search execution_id:
- search summary:
- fetch hint:
- fetch lane:
- fetch execution_id:
- fetch summary:

### 5. /v1/models 检查

- `/v1/models` HTTP status:
- model ids:

### 6. 风险与回滚

- 剩余风险：
- 如需回滚，回滚点：

## 回调质量要求

- 不要只贴测试通过
- 必须给 execution_id、terminal_state、execution_state、summary
- 对 `web_search` 必须给出来源列表或明确说明为什么没有来源
- 如果还有失败，必须给出新的明确 blocker，而不是笼统说仍有异常

---
