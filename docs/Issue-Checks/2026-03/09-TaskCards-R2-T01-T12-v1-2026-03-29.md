# Task Cards: R2 T01-T12

- Date: 2026-03-29
- Status: READY
- Repo: `ironelf`
- Related package: `08-TaskPackage-R2-DMX-Router-Compat-v1-2026-03-29.md`

## A. Config Contract (P0)

## T01

- Output: canonical `dmx` config contract
- DoD: documents the exact env shape, required `/v1` rule, and model field source of truth

## T02

- Output: `dmx` endpoint matrix
- DoD: covers at least official CN, international, and enterprise-style base URLs or explicitly scopes unsupported variants

## T03

- Output: implementation choice note
- DoD: records whether phase 1 is `docs-only via openai_compatible`, `built-in alias/profile`, or `narrow compatibility patch`, with justification

## B. Runtime Compatibility (P0)

## T04

- Output: model-discovery behavior validation
- DoD: verifies how `ironelf` behaves when `${LLM_BASE_URL}/models` succeeds, returns empty, or fails

## T05

- Output: request-execution smoke path
- DoD: proves one real or mock `dmx`-backed completion path works through the configured provider

## T06

- Output: streaming or incremental-response validation
- DoD: confirms whether the current OpenAI-compatible request path works unchanged, or documents the exact gap

## T07

- Output: router error mapping check
- DoD: 401, 429, and 5xx surfaces are turned into structured runtime failures instead of silent hangs

## T08

- Output: timeout and retry note
- DoD: records safe timeout expectations and whether retries should remain at bridge/runtime layer instead of provider layer

## C. UX and Setup (P1)

## T09

- Output: docs update plan
- DoD: `README.md` and `docs/LLM_PROVIDERS.md` changes are identified or implemented with `dmx` samples

## T10

- Output: setup/wizard operator guidance
- DoD: a dev can configure `dmx` from wizard or env without guessing the base URL shape

## D. S9 Acceptance (P0)

## T11

- Output: bridge acceptance note
- DoD: `chimera-core -> S9 -> ironelf(dmx)` has a reproducible smoke or mock-backed verification path

## T12

- Output: handoff summary
- DoD: changed files, commit hash, known limits, and next-step recommendation are included
