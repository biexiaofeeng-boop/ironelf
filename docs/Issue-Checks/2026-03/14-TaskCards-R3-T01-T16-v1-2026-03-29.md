# Task Cards: R3 T01-T16

- Date: 2026-03-29
- Status: READY
- Repo: `ironelf`
- Related package: `13-TaskPackage-R3-Worker-Native-Web-v1-2026-03-29.md`

## A. Capability Boundary (P0)

## T01

- Output: migration boundary note
- DoD: explicitly records what is tool-level migration versus orchestration-level logic left in `chimera-core`

## T02

- Output: Worker tool contract for `web_fetch`
- DoD: input, output, evidence, timeout, and error envelope are defined

## T03

- Output: Worker tool contract for `web_search`
- DoD: provider selection, result format, evidence fields, and fallback semantics are defined

## B. `web_fetch` Native Tool (P0)

## T04

- Output: native `web_fetch` Worker tool
- DoD: performs controlled HTTP fetch, content extraction, and size-limited output

## T05

- Output: `web_fetch` evidence mapping
- DoD: URL, status, content_type, extractor mode, and truncation state are emitted structurally

## T06

- Output: `web_fetch` safety controls
- DoD: timeout, redirect policy, response limit, and allowlist/denylist checks are enforced

## C. `web_search` Native Tool (P0/P1)

## T07

- Output: search provider config surface
- DoD: supports at least primary provider plus optional fallback provider and key injection

## T08

- Output: native `web_search` Worker tool
- DoD: returns normalized title/url/description rows with provider metadata

## T09

- Output: search fallback behavior
- DoD: provider failure can try fallback path when configured, and result records provider_used/fallback_used

## T10

- Output: search evidence/error mapping
- DoD: rate-limit/auth/provider errors become structured runtime output

## D. Runtime Integration (P0)

## T11

- Output: Worker tool registration updates
- DoD: `web_fetch` and `web_search` are visible to Worker runtime and not phantom-routed

## T12

- Output: bridge routing update
- DoD: `tool_hints` sends `web_fetch` and `web_search` to Worker only after native tools exist

## T13

- Output: admission and sandbox policy updates
- DoD: outbound access and required env/config are checked before execution

## E. Validation (P0)

## T14

- Output: focused tests
- DoD: unit/integration coverage exists for `web_fetch`, `web_search`, and route selection behavior

## T15

- Output: checks evidence backfill
- DoD: `15-Checks-R3-Worker-Native-Web-v1-2026-03-29.md` contains concrete validation results

## T16

- Output: dev handoff summary
- DoD: changed files, commit hash, known limits, and explicit note that `browser` remains deferred
